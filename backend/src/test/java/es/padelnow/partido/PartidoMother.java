package es.padelnow.partido;

import es.padelnow.partido.useCases.create.CreatePartidoRequest;
import es.padelnow.partido.*;

import java.util.Date;

public class PartidoMother {

    /*
    public static Partido create(String resultado, Date fecha, String juez_silla, String bolas_breaks, String winners, String smashes, String errores_no_forzados, String bolas_oro, String duracion, String fase){
        return new Partido(resultado, fecha, juez_silla, bolas_breaks, winners, smashes, errores_no_forzados, bolas_oro, duracion, fase);
    }

    public static Partido createFromRequest(CreatePartidoRequest request){
        return new Partido(request.getResultado(), request.getFecha(), request.getJuez_silla(), request.getBolas_breaks(), request.getWinners(), request.getSmashes(), request.getErrores_no_forzados(), request.getBolas_oro(), request.getDuracion(), request.getFase());
    }

    public static Partido random() {
        Date fecha = new Date(System.currentTimeMillis());
        return new Partido("3-1, 2-3, 3-2", fecha, "Pepito Perez", "bolas breaks", "Ganadora: Chanel", "smashes", "errores no forzados", "bolas oro", "3:08:51", "Semifinal");
    }

     */
}
